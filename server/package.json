{
  "name": "mirra-server",
  "version": "1.0.0",
  "type": "module",
  "description": "Mirra API on Railway — Postgres-backed preferences, affirmations and themes. Verifies Cognito JWTs; keeps OpenAI + S3 server-side.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/index.js",
    "migrate": "node src/migrate.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0",
    "express": "^4.21.2",
    "jose": "^5.9.6",
    "pg": "^8.13.1"
  }
}
